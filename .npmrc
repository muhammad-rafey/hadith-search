# React Native / Metro resolve a flat node_modules far more reliably than
# pnpm's default symlinked layout. Expo officially recommends node-linker=hoisted
# for pnpm monorepos (https://docs.expo.dev/guides/monorepos/). This applies to
# the whole workspace; the Next.js web app is unaffected by a flat layout.
node-linker=hoisted
